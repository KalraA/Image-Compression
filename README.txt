README

This program takes in an image, and uses K-means to reduce the number of colors to 11, but that input can be changed. Then it applies dimensionality reduction using the my PCA algorithm to turn the image into a black and white from an RGB image. Just run main.m with octave or matlab.